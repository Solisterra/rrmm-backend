/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove the "X-Powered-By: Next.js" header — don't advertise the stack
  poweredByHeader: false,

  reactStrictMode: true,

  async headers() {
    // Static security baseline. CORS lives in proxy.ts because it must vary
    // by Origin (prod vs. preview deploys vs. localhost), which next.config.js
    // headers() can't express.
    return [
      {
        source: "/:path*",
        headers: [
          { key: "X-Content-Type-Options", value: "nosniff" },
          { key: "X-Frame-Options", value: "DENY" },
          { key: "X-XSS-Protection", value: "0" },
          { key: "Referrer-Policy", value: "strict-origin-when-cross-origin" },
          { key: "X-DNS-Prefetch-Control", value: "off" },
          {
            key: "Permissions-Policy",
            value: "camera=(), microphone=(), geolocation=(), payment=()",
          },
          {
            key: "Strict-Transport-Security",
            value: "max-age=63072000; includeSubDomains; preload",
          },
          { key: "Cross-Origin-Opener-Policy", value: "same-origin" },
        ],
      },
      {
        source: "/api/:path*",
        headers: [{ key: "Cache-Control", value: "no-store" }],
      },
    ];
  },
};

export default nextConfig;
